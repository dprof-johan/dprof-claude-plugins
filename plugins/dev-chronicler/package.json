{
  "name": "dev-chronicler",
  "version": "0.4.5",
  "description": "Claude Code plugin: automatic decision/action/handover chronicle that doubles as agent handover memory.",
  "private": true,
  "scripts": {
    "test": "node --test",
    "validate:json": "node -e \"['.claude-plugin/plugin.json','hooks/hooks.json'].forEach(f=>{try{JSON.parse(require('fs').readFileSync(f,'utf8'));console.log('ok',f)}catch(e){console.error('BAD',f,e.message);process.exit(1)}})\""
  },
  "license": "MIT"
}
